#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 * @argc - array length
 * @argv - arguments passed
 *
 * Return -Always 0 (Success)
 */
int main(int argc, char *argv[])
{
printf("%s\n", *argv[0]);
(void)argc;

return (0);
}
